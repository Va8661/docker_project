version: "3.8"

services:

  webapp:
    build: ./webapp
    container_name: webapp
    restart: always
    expose:
      -8000

  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      -"80:80"    
 